version: '3.3'

services:
   nginx:
    image: automaticacore/automaticacore_proxy:latest
    restart: always
    ports:
      - "80:80"
    links:
      - automatica
   automatica:
    image: automaticacore/automaticacore:latest
    restart: always
    ports:
      - "5001"